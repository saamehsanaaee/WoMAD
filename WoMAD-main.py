"""
    This is where the magic happens.
    This module will use all submodules of the `./WoMAD` directory
    to create the FINAL VERSION of WoMAD.
"""

# STEP 1: Use `data-module.py` to process data

# STEP 2: Use `model-setup-module.py` to create the model

# STEP 3: Use `model-train-module.py` to train the model (duh!)

# STEP 4: Use `model-valid-module.py` to test key metrics

# STEP 5: Use `result-interp-module.py` to collect all metrics and create figures
